<Window x:Class="RadioBloom.Wpf.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:RadioBloom.Wpf"
        Title="RadioBloom"
        Width="1320"
        Height="860"
        MinWidth="1120"
        MinHeight="740"
        WindowStartupLocation="CenterScreen"
        Background="#F5F5F3"
        FontFamily="Segoe UI">
    <Window.Resources>
        <Style x:Key="FilterButtonStyle" TargetType="Button">
            <Setter Property="Width" Value="92" />
            <Setter Property="Height" Value="42" />
            <Setter Property="Padding" Value="0" />
            <Setter Property="Margin" Value="0,0,10,0" />
            <Setter Property="FontSize" Value="13" />
            <Setter Property="FontWeight" Value="SemiBold" />
            <Setter Property="Foreground" Value="#374151" />
            <Setter Property="Background" Value="#F6F7F8" />
            <Setter Property="BorderBrush" Value="#E6E8EC" />
            <Setter Property="BorderThickness" Value="1" />
            <Setter Property="Cursor" Value="Hand" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="14">
                            <ContentPresenter HorizontalAlignment="Center"
                                              VerticalAlignment="Center"
                                              RecognizesAccessKey="True" />
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="PrimaryButtonStyle" TargetType="Button" BasedOn="{StaticResource FilterButtonStyle}">
            <Setter Property="Padding" Value="0" />
            <Setter Property="Width" Value="132" />
            <Setter Property="Height" Value="44" />
            <Setter Property="Background" Value="#111827" />
            <Setter Property="Foreground" Value="White" />
            <Setter Property="BorderBrush" Value="#111827" />
        </Style>

        <Style x:Key="SecondaryButtonStyle" TargetType="Button" BasedOn="{StaticResource FilterButtonStyle}">
            <Setter Property="Padding" Value="0" />
            <Setter Property="Width" Value="104" />
            <Setter Property="Height" Value="44" />
        </Style>

        <Style x:Key="StationItemStyle" TargetType="ListBoxItem">
            <Setter Property="Margin" Value="0,0,0,10" />
            <Setter Property="Padding" Value="0" />
            <Setter Property="BorderThickness" Value="0" />
            <Setter Property="Background" Value="Transparent" />
            <Setter Property="HorizontalContentAlignment" Value="Stretch" />
            <Setter Property="FocusVisualStyle" Value="{x:Null}" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="ListBoxItem">
                        <Border x:Name="ItemChrome"
                                Background="Transparent"
                                BorderBrush="Transparent"
                                BorderThickness="1"
                                CornerRadius="18"
                                SnapsToDevicePixels="True">
                            <ContentPresenter />
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="ItemChrome" Property="Background" Value="#F7F9FC" />
                                <Setter TargetName="ItemChrome" Property="BorderBrush" Value="#D9E2F0" />
                            </Trigger>
                            <Trigger Property="IsSelected" Value="True">
                                <Setter TargetName="ItemChrome" Property="Background" Value="#F2F7FF" />
                                <Setter TargetName="ItemChrome" Property="BorderBrush" Value="#B8D2F2" />
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="MinimalVolumeSliderStyle" TargetType="Slider">
            <Setter Property="Height" Value="20" />
            <Setter Property="Width" Value="170" />
            <Setter Property="Margin" Value="0,2,0,0" />
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Slider">
                        <Grid Margin="0,6,0,6">
                            <Track x:Name="PART_Track">
                                <Track.DecreaseRepeatButton>
                                    <RepeatButton Focusable="False" IsTabStop="False">
                                        <RepeatButton.Template>
                                            <ControlTemplate TargetType="RepeatButton">
                                                <Border Height="4" Background="#D8DEE7" CornerRadius="2" />
                                            </ControlTemplate>
                                        </RepeatButton.Template>
                                    </RepeatButton>
                                </Track.DecreaseRepeatButton>
                                <Track.Thumb>
                                    <Thumb Width="12" Height="12" Background="#111827">
                                        <Thumb.Template>
                                            <ControlTemplate TargetType="Thumb">
                                                <Border Width="12" Height="12" Background="{TemplateBinding Background}" CornerRadius="6" />
                                            </ControlTemplate>
                                        </Thumb.Template>
                                    </Thumb>
                                </Track.Thumb>
                                <Track.IncreaseRepeatButton>
                                    <RepeatButton Focusable="False" IsTabStop="False">
                                        <RepeatButton.Template>
                                            <ControlTemplate TargetType="RepeatButton">
                                                <Border Height="4" Background="#EDF1F5" CornerRadius="2" />
                                            </ControlTemplate>
                                        </RepeatButton.Template>
                                    </RepeatButton>
                                </Track.IncreaseRepeatButton>
                            </Track>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>

    <Grid Margin="28">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2.2*" />
            <ColumnDefinition Width="1*" />
        </Grid.ColumnDefinitions>

        <Grid Grid.Column="0" Margin="0,0,20,0">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>

            <Border Background="White" BorderBrush="#E5E7EB" BorderThickness="1" CornerRadius="22" Padding="24" Margin="0,0,0,18">
                <StackPanel>
                    <TextBlock Text="RadioBloom" FontSize="34" FontWeight="SemiBold" Foreground="#111827" />
                    <TextBlock x:Name="SummaryText" Margin="0,8,0,0" FontSize="14" Foreground="#6B7280" Text="Loading stations..." />
                </StackPanel>
            </Border>

            <Border Grid.Row="1" Background="White" BorderBrush="#E5E7EB" BorderThickness="1" CornerRadius="22" Padding="20" Margin="0,0,0,18">
                <Grid>
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*" />
                        <ColumnDefinition Width="280" />
                    </Grid.ColumnDefinitions>
                    <ScrollViewer HorizontalScrollBarVisibility="Hidden" VerticalScrollBarVisibility="Disabled">
                        <StackPanel x:Name="CategoryPanel" Orientation="Horizontal" />
                    </ScrollViewer>
                    <TextBox x:Name="SearchBox" Grid.Column="1" Height="42" Padding="14,10" FontSize="14" BorderBrush="#E5E7EB" BorderThickness="1" Background="#FAFAF9" />
                </Grid>
            </Border>

            <Border Grid.Row="2" Background="White" BorderBrush="#E5E7EB" BorderThickness="1" CornerRadius="22" Padding="8">
                <ListBox x:Name="StationList" Background="Transparent" BorderThickness="0" HorizontalContentAlignment="Stretch" ScrollViewer.HorizontalScrollBarVisibility="Disabled" SelectionMode="Single">
                    <ListBox.ItemContainerStyle>
                        <StaticResource ResourceKey="StationItemStyle" />
                    </ListBox.ItemContainerStyle>
                    <ListBox.ItemTemplate>
                        <DataTemplate DataType="{x:Type local:RadioStation}">
                            <Border x:Name="Card"
                                    Margin="1"
                                    Padding="18,16"
                                    Background="#FCFCFB"
                                    BorderBrush="#E7E8EA"
                                    BorderThickness="1"
                                    CornerRadius="18">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*" />
                                        <ColumnDefinition Width="Auto" />
                                    </Grid.ColumnDefinitions>
                                    <StackPanel>
                                        <TextBlock Text="{Binding Name}" FontSize="18" FontWeight="SemiBold" Foreground="#111827" />
                                        <TextBlock Text="{Binding Location}" Margin="0,6,0,0" FontSize="13" Foreground="#667085" />
                                        <TextBlock Text="{Binding Tagline}" Margin="0,10,0,0" FontSize="13" Foreground="#344054" TextWrapping="Wrap" />
                                    </StackPanel>
                                    <Border Grid.Column="1"
                                            Height="28"
                                            MinWidth="74"
                                            Margin="18,0,0,0"
                                            Padding="12,0"
                                            Background="#F4F6F8"
                                            CornerRadius="14"
                                            VerticalAlignment="Top">
                                        <TextBlock Text="{Binding Genre}"
                                                   HorizontalAlignment="Center"
                                                   VerticalAlignment="Center"
                                                   FontSize="12"
                                                   Foreground="#667085" />
                                    </Border>
                                </Grid>
                            </Border>
                            <DataTemplate.Triggers>
                                <DataTrigger Binding="{Binding RelativeSource={RelativeSource AncestorType=ListBoxItem}, Path=IsMouseOver}" Value="True">
                                    <Setter TargetName="Card" Property="Background" Value="#F9FBFE" />
                                    <Setter TargetName="Card" Property="BorderBrush" Value="#D5E3F5" />
                                </DataTrigger>
                                <DataTrigger Binding="{Binding RelativeSource={RelativeSource AncestorType=ListBoxItem}, Path=IsSelected}" Value="True">
                                    <Setter TargetName="Card" Property="Background" Value="#F4F8FF" />
                                    <Setter TargetName="Card" Property="BorderBrush" Value="#B7D0F2" />
                                </DataTrigger>
                            </DataTemplate.Triggers>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
            </Border>
        </Grid>

        <Border Grid.Column="1" Background="White" BorderBrush="#E5E7EB" BorderThickness="1" CornerRadius="22" Padding="24">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <TextBlock x:Name="StatusText" FontSize="12" FontWeight="SemiBold" Foreground="#2563EB" Text="Ready" />
                <TextBlock x:Name="TitleText" Grid.Row="1" Margin="0,18,0,0" FontSize="28" FontWeight="SemiBold" Foreground="#111827" Text="Select a station" />
                <TextBlock x:Name="SubtitleText" Grid.Row="2" Margin="0,8,0,0" FontSize="14" Foreground="#6B7280" Text="Minimal radio for Windows" />
                <TextBlock x:Name="DescriptionText" Grid.Row="3" Margin="0,18,0,0" FontSize="14" Foreground="#374151" TextWrapping="Wrap" Text="Choose a station on the left to begin." />

                <StackPanel Grid.Row="4" Margin="0,22,0,0" Orientation="Horizontal">
                    <Button x:Name="PlayButton" Style="{StaticResource PrimaryButtonStyle}" Content="Play" />
                    <Button x:Name="StopButton" Style="{StaticResource SecondaryButtonStyle}" Margin="10,0,0,0" Content="Stop" />
                </StackPanel>

                <StackPanel Grid.Row="6" Margin="0,22,0,0">
                    <Slider x:Name="VolumeSlider" Style="{StaticResource MinimalVolumeSliderStyle}" Minimum="0" Maximum="100" Value="72" />
                    <TextBlock x:Name="SourceText" Margin="0,14,0,0" FontSize="12" Foreground="#9CA3AF" TextWrapping="Wrap" Text="Source metadata will appear here." />
                </StackPanel>
            </Grid>
        </Border>
    </Grid>
</Window>
